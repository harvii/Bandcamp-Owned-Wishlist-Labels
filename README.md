## Bandcamp Owned/Wishlist Labels

Firefox add-on that adds "You own this" / "In Wishlist" badges to Bandcamp grid views (artist/label discography pages) by reading the logged-in user's collection data from Bandcamp's own /api/fan/2/collection_summary endpoint. No data is collected, stored, or transmitted anywhere outside the user's own browser session with bandcamp.com.